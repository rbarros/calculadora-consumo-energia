services:
  - name: calculadora-energia-gun-relay
    type: web
    runtime: node
    plan: free
    rootDir: relay
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /gun
